JFET transfer characteristics

Separate gate and drain sources expose the JFET device characteristic.

A more negative gate to source voltage reduces drain current. The gate junction remains reverse biased during these sweeps.

The output curves show the transition from the resistive region to saturation. The transfer curve shows current versus gate voltage.

The channel-length modulation parameter gives the saturation region a finite slope.

Extract every file into one folder. Open jfet-transfer-curves.asc and select Run.
Use View > Step Legend to identify parameter steps.
If no plot appears, open jfet-transfer-curves.plt through Plot Settings > Open Plot Settings File.
Default traces: Id(J1)

Equal node labels connect. All semiconductor parameters are in the schematic.
Op-amp circuits use UniversalOpamp2, supplied with LTspice. No separate download is needed.
The CIR is the equivalent netlist. The CSV contains original saved samples.
AC data use real and imaginary columns. Time values include the RAW offset.
The website interpolates saved samples. It does not execute LTspice.
